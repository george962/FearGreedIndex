name: Update Fear and Greed Google Sheet

run-name: Update Fear and Greed Google Sheet

on:
  push:
    branches:
      - main

  schedule:
    - cron: "3-59/5 * * * 1-5"

concurrency:
  group: fear-greed-sheet
  cancel-in-progress: false

jobs:
  update-sheet:
    name: Update Fear and Greed Google Sheet
    runs-on: ubuntu-latest
    timeout-minutes: 4

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install certifi gspread google-auth

      - name: Update Google Sheet
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          GOOGLE_SPREADSHEET_NAME: FearAndGreed
          GOOGLE_WORKSHEET_NAME: Sheet1
        run: python FearGreed.py --update-sheet